# Neon PostgreSQL URL (also used as NETLIFY_DATABASE_URL on Netlify)
# Copy to .env and fill in your Neon connection string (do not commit .env)
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Server
PORT=4001
NODE_ENV=development

# URLs (für CORS)
FRONTEND_URL=http://localhost:5173

# Session (starkes Geheimnis in Produktion setzen)
SESSION_SECRET=dein-sicheres-zufallsgeheimnis

# Groq API Key (kostenlos für USP-Extraktion)
# Erstelle einen Account auf https://console.groq.com/ und generiere einen API Key
GROQ_API_KEY=gsk_dein-groq-api-key-hier
